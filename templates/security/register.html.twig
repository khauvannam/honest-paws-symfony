{% extends 'base.html.twig' %}
{% block title %}Register{% endblock %}

{% block body %}
    <div class=" w-full flex justify-center text-main">
        <div class="w-full flex">
            <div class="w-1/2">
                <div class="w-full h-[100vh] bg-cover bg-center"
                     style="background-image: url('{{ asset('img/Register/register-img.jpg') }}');">
                </div>
            </div>
            <div class="flex w-1/2 pt-[10%] px-[5%]">
                <div class=" w-full my-7 mx-10 bg-white rounded-lg ">
                    <h1 class="text-2xl font-bold mb-4 text-center">Create Account</h1>
                    {{ form_start(form, { 'attr': { 'class': 'form' } }) }}
                    <div class="mb-4">Username:
                        {{ form_widget(form.username, { 'attr': { 'class': 'form-control mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm' } }) }}
                    </div>
                    <div class="mb-4">Email:
                        {{ form_widget(form.email, { 'attr': { 'class': 'form-control mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm' } }) }}
                    </div>
                    <div class="mb-4">Password:
                        {{ form_widget(form.password, { 'attr': { 'class': 'form-control mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm' } }) }}
                    </div>
                    <div>
                        {{ form_widget(form.register, { 'attr': { 'class': 'w-full inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500' } }) }}
                    </div>
                    {{ form_end(form) }}
                    <div class="mt-4 w-full flex justify-end">
                        <button class="hover:underline" type="button"
                                onclick="window.location.href='{{ path('login') }}'">
                            Already have an account?
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock body %}
