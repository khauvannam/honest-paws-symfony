

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Reset Password Request</h1>
    {{ form_start(form) }}
        {{ form_row(form.email) }}
        <button class="btn">Send Reset Link</button>
    {{ form_end(form) }}
{% endblock %}
